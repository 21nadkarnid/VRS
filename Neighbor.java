public class Neighbor {
  public HouseManager NN(HouseManager path) {
    House home = new House(125, 22, a);
    House current = home;
    int minDist = 65536;
    for (int i = 0; i < path.getList().size(); i++) {
      if (path.getList().get(i).distanceTo(current) < minDist) {
        
    }
  }
}
